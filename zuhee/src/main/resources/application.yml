server:
  port: 8012
  address: 0.0.0.0
spring:
  application:
    name: zuhee

eureka:
  instance:
    hostname: 18.116.248.2
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"